# settings-showa: the settings screen in the showa palette.
#
# Generated by scripts/dev/tapes.sh. Do not edit: the Set Theme block below
# is the output of `engram theme export showa`, so a hand-written hex
# here would drift from the palette the binary actually resolves.
#
# The window is 1200x900 rather than the 1200x700 a still frame would
# suggest, and the padding is cut to 20px. The workspace runs in the alternate
# screen, so the shell prompt and the typed command are not in the picture —
# what the extra rows buy is a terminal tall enough for the screens that have
# the most to say, drawn at the font size below.
#
# The .gif named by Output is a by-product. VHS records into the file Output
# names and writes a Screenshot out of the recording, so a tape that only
# wants a still frame still has to name a recording — and the Screenshot is
# only written once a later command has advanced the recording past it, which
# is what the trailing Sleep is for.
#
# Render it with the shots profile, from the repository root:
#   docker compose -f docker-compose.dev.yml --profile shots run --rm vhs tapes/settings-showa.tape

Output "/work/out/settings-showa.gif"

Set Width 1200
Set Height 900
Set Padding 20
Set FontSize 16
Set FontFamily "JetBrainsMono NFM"
Set Theme {"background":"#0f0f11","black":"#0f0f11","blue":"#7cb8dd","brightBlack":"#72727c","brightBlue":"#7cb8dd","brightCyan":"#8ad7c8","brightGreen":"#9ec97e","brightMagenta":"#f5d6c6","brightRed":"#ff7a86","brightWhite":"#f2efe9","brightYellow":"#e6b455","cursor":"#ff8552","cyan":"#8ad7c8","foreground":"#f2efe9","green":"#9ec97e","magenta":"#f5d6c6","red":"#ff7a86","selection":"#1c1c20","white":"#a8a49c","yellow":"#dcb43f"}

Env ENGRAM_DATA_DIR "/data"
Env ENGRAM_VAULT_ROOT "/vault"
Env ENGRAM_TUI_ICONS "nerd"
Env ENGRAM_TIMEZONE "UTC"
Env LANG "C.UTF-8"

Type "engram tui --project koi-garden --theme showa"
Enter
Sleep 3s
Type "7"
Sleep 2s

Screenshot "/work/out/settings-showa.png"
Sleep 1s
