services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - engram-cloud-pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U engram -d engram_cloud"]
      interval: 10s
      timeout: 5s
      retries: 10

  cloud:
    # ghcr.io/horacioespinosa/engram — this fork's own image, not
    # ghcr.io/gentleman-programming/engram (upstream, no ClaroDrive cloud
    # configuration or this fork's fixes). :custom always tracks the latest
    # release; pin an explicit version tag instead for a production deploy.
    image: ghcr.io/horacioespinosa/engram:custom
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env
    ports:
      - "18080:18080"

volumes:
  engram-cloud-pg:
