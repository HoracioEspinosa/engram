name: Publish gentle-engram

on:
  push:
    tags:
      - "pi-v*"
  workflow_dispatch:

jobs:
  publish:
    # Publishes the npm package `gentle-engram`, which belongs to upstream.
    # A fork that runs this releases under upstream's name, so the job is
    # fenced to the upstream repository the same way release.yml and
    # cloud-image.yml are. To publish from this fork, this is the line to
    # change — deliberately, not by inheriting a tag push.
    if: github.repository == 'Gentleman-Programming/engram'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Ensure npm supports OIDC trusted publishing (>= 11.5.1)
        run: |
          CURRENT=$(npm --version)
          echo "Bundled npm: $CURRENT"
          if [ "$(printf '%s\n11.5.1\n' "$CURRENT" | sort -V | head -1)" != "11.5.1" ]; then
            echo "Upgrading to npm@11.5.1 for OIDC support..."
            npm install -g npm@11.5.1
          fi
          echo "Active npm: $(npm --version)"

      - name: Publish to npm with provenance
        working-directory: plugin/pi
        run: npm publish --provenance --access public
