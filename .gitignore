# Binary (only at root, not cmd/engram/ directory)
/engram
*.exe

# Database
*.db
*.db-wal
*.db-shm

# Export files
engram-export.json

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# AI Skills symlinks (generated by setup.sh)
.claude/skills
.codex/skills
.github/skills
.gemini/skills
engram-dev
.atl/
.release-notes-beta.md

# The locally built binary the MCP config points at. It is 28 MB and it is
# rebuilt from source, so it never belongs in the history.
bin/
